// Public photos for the terminal's gallery program and photo viewer: { id, file, title, src }.
// Empty for now; the terminal hides ~/photos and the gallery command while it is.
export const photoCatalog = Object.freeze([]);
