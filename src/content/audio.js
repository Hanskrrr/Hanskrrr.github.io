// Public audio for the terminal's player: { id, file, title, src, duration }.
// Empty for now; the terminal hides ~/audio and the player command while it is.
export const audioTracks = Object.freeze([]);
